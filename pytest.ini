[pytest]
# The suite lives beside the package it tests. `pythonpath = .` puts the
# repository root on sys.path so `from backend.identity import ...` resolves
# the same way it does when the service runs — the original relied on a
# conftest.py at the repo root to do this, which was Engineering Tools'.
testpaths = backend/identity/tests
pythonpath = .
